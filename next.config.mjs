/** @type {import('next').NextConfig} */
const nextConfig = {
	output: "export",
	images: {
		unoptimized: true,
	},
	basePath: "/javascript-frameworks-assignment",
	assetPrefix: "/javascript-frameworks-assignment/",
};

export default nextConfig;
